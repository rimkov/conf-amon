#!/bin/bash

# pre-intallation pour conf-amon

# suppression des anciens dictionnaires (amon-2.0)
for dico in amon authentification prelude squid
do
    fic=/etc/eole/dicos/$dico.xml
    if [ -f $fic ]
    then
        rm -f $fic
    fi
done

#DEBHELPER#
