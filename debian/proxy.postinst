#!/bin/bash

if [ "$1" != "configure" ]; then
    exit 0
fi

#enable, but only on new installs.
if [ -z "$2" ]; then
    /usr/share/eole/schedule/manage_schedule post majblacklist daily add
fi

exit 0
