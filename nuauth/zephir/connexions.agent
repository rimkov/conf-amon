# -*- coding: UTF-8 -*-
"""
agent conntrack pour nu-fw
"""

# période de 1 minute
period = 60

# instanciation de l'agent si conntrack est disponible
try:
    import pyctd
except:
    AGENTS = []
else:
    from zephir.monitor.agents.connexions import Connexions
    ct = Connexions('connexions', period = period, section="""Utilisation""",
                    description = """Monitoring des connexions (NetFilter)""")
    AGENTS = [ct]
